name: flutter_easy_swagger_generator
description: "A Flutter package for generating Dart models and API clients from Swagger/OpenAPI specifications."
version: 1.0.3
homepage: https://github.com/JamalSorani/flutter_easy_swagger_generator
repository: https://github.com/JamalSorani/flutter_easy_swagger_generator
environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  dartz: ^0.10.1
  dio: ^5.9.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_riverpod: ^2.6.1
  freezed_annotation: ^3.1.0
  get_it: ^8.2.0
  internet_connection_checker: ^3.0.1
  reactive_forms: ^17.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  lints: ^4.0.0
  test: ^1.24.0

executables:
  flutter_easy_swagger_generator: flutter_easy_swagger_generator
  # 👆 Makes your CLI available using:
  # dart run flutter_easy_swagger_generator

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
